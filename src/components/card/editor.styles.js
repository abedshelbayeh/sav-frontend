import styled from "styled-components";

export const Textarea = styled.div`
  width: 100%;
  word-break: break-word;
  hyphens: auto;
  outline: none;
  resize: none;
  font-weight: 500;
  padding: 0 30px 0 0;
  cursor: text;
`;
